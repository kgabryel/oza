{% block password_widget %}
    <input-text-field
        label='{{ label }}'
        type='password'
        name='{{ full_name }}'
        value='{{ value }}'
        errors='{{ errors|errorMessages|json_encode() }}'
        {% if hint is defined %}bottom='{{ hint }}'{% endif %}
        {% if attr.maxlength is defined %}maxlength='{{ attr.maxlength }}'{% endif %}>
    </input-text-field>
{% endblock %}