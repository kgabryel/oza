<style>
    .container {
        padding: 5px;
        background-color: #F7F8F9;
    }

    .content {
        width: 600px;
        min-width: 600px;
        background-color: white;
    }

    .banner {
        margin-bottom: 5px;
    }

    .inner-content {
        padding-left: 15px;
        padding-right: 15px;
        padding-bottom: 15px;
    }

    .action-button {
        height: 44px;
        color: #fff;
        background-color: #1565c0;
        border-color: #1565c0;
        box-shadow: 0 3px 1px -2px rgba(0, 0, 0, .2), 0 2px 2px 0 rgba(0, 0, 0, .14), 0 1px 5px 0 rgba(0, 0, 0, .12);
        text-decoration: none;
        text-align: center;
        font-size: .875rem;
        font-weight: bold;
        width: 570px;
        align-items: center;
        border-radius: 4px;
        display: inline-flex;
        flex: 0 0 auto;
        letter-spacing: .0892857143em;
        justify-content: center;
        outline: 0;
        position: relative;
        text-indent: 0.0892857143em;
        text-transform: uppercase;
        transition-duration: .28s;
        transition-property: box-shadow, transform, opacity;
        transition-timing-function: cubic-bezier(.4, 0, .2, 1);
        -webkit-user-select: none;
        -moz-user-select: none;
        -ms-user-select: none;
        user-select: none;
        vertical-align: middle;
        white-space: nowrap;
        margin-bottom: 5px;
    }
</style>
<table width="100%" border="0" cellspacing="0" cellpadding="0" class='container'>
    <tr>
        <td align="center">
            <div class='content'>
                <img src='{{ absolute_url(asset('img/banner.png')) }}' class='banner'/>
                <br/>
                <div class='inner-content'>
                    <h1>
                        Wiadomość wysłana w związku z próbą resetu hasła.
                    </h1>
                    <p>
                        W celu zresetowania hasła kliknij w poniższy link:
                    </p>
                    <a
                        href="{{ url('resetPassword.showChangePassword', {token: resetToken.token}) }}"
                        class='action-button'>
                        Resetuj hasło
                    </a>
                    <p>Link wygaśnie po godzinie.</p>
                </div>
            </div>
        </td>
    </tr>
</table>
